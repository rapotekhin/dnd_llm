# Release Notes

## Version 0.3.0

### Улучшения UI

- **Возврат из диалога в исследование стал понятным**
  - В чате появляется серый разделитель «— Возвращение к исследованию —», чтобы было видно, где заканчивается побочный эпизод
  - Под ним — короткая сводка от DM о том, что произошло в беседе с NPC
  - После сводки — свежее описание сцены, написанное именно как продолжение (NPC может уже отойти, обстановка измениться — это видно в описании), а не как первый кадр

### Исправления

- **DM больше не «забывает» только что закончившийся диалог**
  - Раньше после выхода из беседы с NPC мастер мог как ни в чём не бывало продолжать разговор в окне исследования или предлагать действия, не учитывающие, что игрок только что наговорил
  - Кнопка «Уйти» из диалога теперь корректно дожидается записи итога беседы (1-2 секунды «думает…») — итог больше не теряется

- **Диалоги с NPC перестали зацикливаться**
  - DM больше не выпрыгивает с уточняющими вопросами на каждый ответ игрока («о чём именно расспросить?»)
  - Если ввод игрока неоднозначный, NPC сам переспрашивает в своём характере — диалог идёт нормально

- **Стабильная LLM-модель по умолчанию**
  - Дефолтная модель сменена с preview-варианта на стабильную ветку (`google/gemini-3.1-flash-lite`)
  - Preview-варианты периодически теряют провайдера и валятся в ошибку 404; стабильная — нет

### Технические улучшения

- **Полное логгирование ошибок и игрового потока в терминал**
  - Любые сбои, в том числе в фоновых потоках диалога и исследования, выводятся с полным трейсбэком — раньше такие ошибки молча терялись
  - Ключевые события (переходы между режимами, старт/стоп фоновых потоков, генерация сводок) пишутся на уровне INFO — проще диагностировать поведение игры
  - Уровень логирования настраивается переменной окружения `DND_LOG_LEVEL`

## Version 0.2.6

### Исправления

- **Зависимости**
  - В `requirements.txt` для `FantasyNameGenerator` указана версия с PyPI — `0.0.5`; ранее зафиксированная `0.1.2` на PyPI не существует, из‑за чего падали `pip install` и CI

## Version 0.2.5

### Технические улучшения

- **CI на GitHub**
  - При каждом pull request в `main` автоматически запускаются юнит-тесты (`pytest`)
  - В PR должны изменяться `__version__` в `game/__init__.py` и файл `RELEASE_NOTES.md`, иначе проверка CI не проходит

## Version 0.2.4

### Технические улучшения

- **Автоматические проверки ключевой логики**
  - Добавлен набор тестов без запуска клиента игры: создание персонажа и поднятие уровня, торговля и инвентарь, сохранения и загрузка состояния мира, загрузка стартовых локаций и NPC из данных проекта
  - Сценарии исследования и разговора с персонажами можно прогонять с подменой ответов нейросети, без ключа OpenRouter — проще ловить регрессии до ручной проверки
  - В README добавлены инструкции по установке зависимостей для тестов и опциональному просмотру покрытия кода

## Version 0.2.3

### Исправления

- **Экран создания персонажа**
  - Устранён сбой на шаге подтверждения после перестройки кода интерфейса

### Технические улучшения

- **Структура крупных экранов Pygame**
  - Создание персонажа, торговля и поднятие уровня разнесены по подмодулям — проще сопровождать и развивать интерфейс без смены привычных точек входа в коде

## Version 0.2.2

### Технические улучшения

- **Работа нейросети в игре**
  - Упрощена внутренняя цепочка запросов к модели: один устойчивый способ без лишних промежуточных слоёв
  - При установке подтягивается меньше сторонних библиотек — проще окружение и ниже риск конфликтов версий

- **Локализация**
  - Русский и английский наборы строк выровнены по одному списку подписей, чтобы при переключении языка реже появлялись технические заглушки вместо текста

## Version 0.2.1

### Технические улучшения

- **Полная документация проекта**
  - Создана структура `/docs` с разделами: vision, design, narrative, tech, production
  - Описаны игровые системы, архитектура, модели данных и LLM-интеграция
  - Зафиксированы архитектурные решения (ADR) и роадмап до версии 1.0

- **Интеграция с AI-ассистентами для разработки**
  - Добавлены скиллы для Claude Code: чтение документации, обновление документации, ведение release notes, создание новых скиллов
  - Зеркальные правила для Cursor в `.cursor/rules/`
  - Единый источник проектного контекста через `AGENTS.md`

- **Обновлены `README.md` и `CLAUDE.md`** — приведены в соответствие с актуальным состоянием проекта, удалено устаревшее упоминание Streamlit-прототипа и неиспользуемых LLM-провайдеров

## Version 0.2.0

### Новые функции

- **Система поднятия уровня персонажа**
  - Добавлен экран поднятия уровня с пошаговым выбором новых возможностей
  - Проверка опыта для определения возможности поднятия уровня
  - Подсветка кнопки "Персонаж" золотым цветом, когда доступно поднятие уровня
  - Выбор новых особенностей (features) с поддержкой выбора подособенностей (subfeatures)
  - Улучшение характеристик (ability score improvements)
  - Выбор новых заклинаний и заговоров с ограничениями согласно правилам D&D 5e
  - Выбор новых навыков (proficiency choices) только из данных уровня
  - Экран подтверждения с итоговой информацией о выборе
  - Автоматическое обновление `class_specific` характеристик (action_surges, sorcery_points, etc.)

### Улучшения UI

- **Исправление перекрытия элементов интерфейса**
  - Стандартизирован порядок отрисовки элементов (z-order) во всех экранах
  - Модальные окна теперь рисуются после всех элементов с затемняющим overlay
  - Тултипы всегда рисуются последними, поверх всех элементов
  
- **Адаптивное позиционирование элементов**
  - Счетчики способностей: кнопки "+/-" теперь позиционируются динамически после текста метки
  - Экран "Биография": метки размещаются выше соответствующих кнопок и полей ввода
  - Список мировоззрений: ограничен высотой и не выходит за пределы экрана
  - Списки выбранных элементов (заклинания, навыки): позиционируются ниже списков доступных элементов
  - Все элементы адаптивно позиционируются с учетом границ экрана и кнопок навигации

- **Исправления в экранах**
  - Экран "Способности": текст больше не перекрывает кнопки "+/-"
  - Экран "Биография": текст меток не перекрывает кнопки и поля ввода
  - Экран поднятия уровня: текст заголовков не перекрывается с индикаторами этапов
  - Модальные окна выбора подособенностей: добавлена прокрутка для длинных списков
  - Все экраны: элементы ограничены областями, чтобы не перекрывать кнопки навигации

### Технические улучшения

- Добавлен класс `LevelUpBuild` для управления выбором при поднятии уровня
- Добавлены утилиты `level_up_utils.py` для проверки возможности поднятия уровня
- Улучшена обработка `class_specific` данных в `Character` и `CharacterBuild`
- Добавлено клиппирование текста в панелях информации
- Улучшена обработка событий в модальных окнах с поддержкой прокрутки

## Version 0.1.0

- Начальная версия игры
